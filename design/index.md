# design/ — index

UI/UX exploration artifacts — visual mockups and design references. This
folder is **not loaded by the app** (nothing under `src/` imports from
here) and contains no production code. Purely for design review and
history.

## Contents

| Name | Type | Responsibility (one line) | Status |
|---|---|---|---|
| `mockups/` | folder | Standalone HTML mockups of individual app screens — see `mockups/index.md` | ⚠️ needs verification (design exploration, not implementation) |

## Depends on
Nothing.

## Depended on by
Nothing — not part of the app's dependency graph.

## Note
This folder was added outside the original tree documented in the root
`CLAUDE.md` §5 directory map. If you want it formally part of the
project's structure, add a line for `design/` there too.
