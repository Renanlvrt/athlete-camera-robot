# Dependencies
node_modules/

# Expo
.expo/
dist/
web-build/
expo-env.d.ts

# Native projects — regenerated by `expo prebuild` (Continuous Native Generation).
# These MUST stay untracked: CI regenerates them from app.json on every run, and a
# committed copy would silently build stale native code. See docs/PRD.md §3.2.
ios/
android/

# Build artifacts
*.ipa
*.apk
*.aab
*.tar.gz

# macOS / Windows cruft
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/

# Logs
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# Local env
.env
.env.local
